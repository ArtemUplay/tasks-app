import { styled } from 'styled-components';

export const StyledPageContainerBox = styled.main({
  boxSizing: 'border-box',
  flex: '1 1 auto',
  padding: '15px 0',
  width: '700px',
  display: 'flex',
  flexDirection: 'column',
  minHeight: '100vh',
});
